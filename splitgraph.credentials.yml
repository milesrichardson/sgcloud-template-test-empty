credentials:
  socrata:  # This is the name of this credential that "external" sections can reference.
    plugin: socrata
    # Credential-specific data matching the plugin's credential schema
    data:
      app_token: ''  # Socrata app token
  dbt-sample:
    plugin: dbt
    data:
      git_url: $THIS_REPO_URL
